title: Prototype Chain
level 1 : Object prototype // Object.create()
level 2 :
  - Array prototype // Array.push()
  - Number prototype // Number.toFixed()
  - String prototype // String.toUpperCase();
  - Function prototype // Function.toSource();
title2: Syntactic sugar
  definition: In computer science, syntactic sugar is syntax within a programming language, that is designed to make things easier to read or to express.
#  const obj1 = {};
#  const obj2 = Object.create(Object.prototype);
#  console.log(Object.prototype.isPrototypeOf(obj1));
#  console.log(Object.prototype.isPrototypeOf(obj2));
